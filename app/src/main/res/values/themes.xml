<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.App.Splash" parent="Theme.SplashScreen">
        <!-- Splash background -->
        <item name="windowSplashScreenBackground">@color/splash</item>

        <!-- App logo -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_te</item>

        <!-- After splash -->
        <item name="postSplashScreenTheme">@style/Theme.Writo</item>
    </style>


    <style name="Theme.Writo" parent="android:Theme.Material.Light.NoActionBar" />
</resources>